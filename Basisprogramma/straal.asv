function [r] = straal(m) %massa in aardmassa's
    rho = 3.1093 * 10^6; %= 5.5 / 10^3 /(5.97 * 10^24) * (1.5*10^11)^3; %in aardmassa's / AE^3
    V = m/rho; %in AE^3
    r = (3*V/(4*pi))^(1/3);
    
    
end